version: "3.8"

services:
  streamlit-app:
    image: pdemeulenaer/rag-frontend:latest
    environment:
      - API_URL=STAGING_API_URL    
    restart: always

  api:
    image: pdemeulenaer/rag-backend:latest
    restart: always
