version: "3.8"

services:
  streamlit-app:
    image: pdemeulenaer/rag-frontend:latest
    # environment:
    #   - API_URL=https://rag-demo-pdemeulenaer-g6fmh2fjhthea2b5-staging.northeurope-01.azurewebsites.net/rag2
    environment:
      - API_URL=STAGING_API_URL    
    restart: always

  api:
    image: pdemeulenaer/rag-backend:latest
    restart: always
